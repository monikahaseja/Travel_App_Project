import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 16
    },
    icon: {
        width: 40,
        height: 40,
    },
    title: {
        fontSize: 15,
        color: '#000',
        marginHorizontal: 8,
        fontWeight: 'bold',
        marginRight: 32,
    }
});

export default styles;